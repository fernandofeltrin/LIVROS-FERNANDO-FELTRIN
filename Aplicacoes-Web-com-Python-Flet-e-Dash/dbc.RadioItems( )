import dash
from dash import html, dcc
import dash_bootstrap_components as dbc

html.Div([
    dbc.RadioItems(
        id = '',
        options = [{'label':'', 'value':''},
                   {'label':'', 'value':''},
                   {'label':'', 'value':''}],
        value = '',
        inline = True
    )
])
