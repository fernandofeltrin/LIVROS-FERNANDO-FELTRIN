import dash
from dash import html, dcc
import dash_bootstrap_components as dbc

html.Div([
    dcc.Checklist(
        id = '',
        options = [
            {'label':'', 'value':''},
            {'label':'', 'value':''},
            {'label':'', 'value':''}
        ]
    )
])
